- Consider easy customizing (llm model choice and parameters), probably in the Gradio interface.
- Include a "developers" tab in the interface, for all the model selection, parameters, etc.
- Allow to load an already existing vector store, and update it with embedings from new notes.
- Add the option to include/exclude thinking in the output. Also to force the model not to reason (or decide how much?)
- Look in the code for other imports that may require package installation in the venv
- Include fallback models (HuggingFaceTB/SmolLM3-3B for instance)
- Can I remove the Ctrl + click in terminal to start the interface, and leave only click?
- Check whether I can save token length by not returning COT of the model
- Why the interface sometimes do not process documents, depending on my desktop activity?
- Allow usage of local model. For example with OLLAMA. But make sure that the default model is lightweight so it can fit in no-GPU machines with, say, 16GB RAM.
- Format generated response (the raw LLM response) to the requested file type.
- Explore inference methods other than InferenceClient.text_generation (nor working) or .chat_completion
- Comment on the usage of FAISS
- Add .env_example
- Make Gradio interface look nicer
- Make the system find bibliography to cite that is not specifically mentioned in the notes
- When the notes mention ideas by someone else, make the system verify the accuracy of the attribution (and if possible add citation).
- Fine-tune a model, or find a model fine-tuned for this specific task.
- Allow to remove files from store by selecting them on click (instead of only by number)
- Remove the excessive "not present in notes" usage in the llm's output
